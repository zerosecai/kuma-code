# Skill System — Placeholder

The full skill-system code lives in your earlier prototype:
  ~/Projects/skill-system/

To finish integration, run from the repo root:

  cp -r ../skill-system/src/*  packages/skill-system/src/
  cp -r ../skill-system/docs/* packages/skill-system/docs/
  cp -r ../skill-system/skills packages/skill-system/skills

Or if your prototype is somewhere else, adjust the path.
